{
  "name": "@highpoint/bs-js-fetch",
  "version": "0.2.0",
  "description": "BuckleScript bindings for HighPoint Fetch Library",
  "main": "src/Fetch.re",
  "repository": {
    "type": "git",
    "url": "git+https://github.com:hp-mobile/bs-js-fetch.git"
  },
  "author": "HighPoint <support@mhighpoint.com>",
  "license": "MIT",
  "keywords": [
    "reason",
    "bucklescript",
    "highpoint",
    "fetch"
  ],
  "scripts": {
    "build": "bsb -make-world",
    "clean": "bsb -clean-world",
    "start": "bsb -make-world -w",
    "pretest": "rimraf tests/main.bs.js && yarn build && sed -i '1s;^;require(\"@babel/polyfill\");\n' tests/main.bs.js",
    "test": "browserify tests/main.bs.js | tape-run --browser chrome --render=faucet"
  },
  "dependencies": {
    "@highpoint/js-fetch": "^0.1.3"
  },
  "devDependencies": {
    "@babel/polyfill": "^7.0.0-beta.55",
    "browserify": "^16.2.2",
    "bs-platform": "^4.0.2",
    "bs-tape": "^0.3.0",
    "faucet": "^0.0.1",
    "rimraf": "^2.6.2",
    "tape-run": "^4.0.0"
  },
  "files": [
    "src/*.re",
    "src/*.rei",
    "bsconfig.json"
  ]
}
